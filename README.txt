Step to run the project:

The Project is present in the folder "ccms".
1. Import the file "ccmsDatabase.sql" from database folder into a database named "ccmsDatabase".
2. The name and password for a normal user is 'admin` and 'admin' respectively.
3. You'll first have to make a user to continue, this can be done  by sign up.
4. More details are given in the PDF.
5. Also go through the screenshots to have a glance at the pages avaiable.